apiVersion: batch/v1
kind: Job

metadata:
  name: demo-job
  namespace: ecommerce

spec:
  template:
    spec:
      containers:
      - name: demo-job
        image: busybox
        command:
        - sh
        - -c
        - echo "Hello from Kubernetes Job"

      restartPolicy: Never

  backoffLimit: 2

