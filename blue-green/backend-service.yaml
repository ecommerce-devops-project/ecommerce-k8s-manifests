apiVersion: v1
kind: Service

metadata:
  name: backend-bluegreen-service
  namespace: ecommerce

spec:
  selector:
    app: backend
    version: blue

  ports:
  - port: 80
    targetPort: 80

  type: ClusterIP
