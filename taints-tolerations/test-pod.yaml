apiVersion: v1
kind: Pod

metadata:
  name: test-pod
  namespace: ecommerce

spec:
  tolerations:
    - key: "training"
      operator: "Equal"
      value: "true"
      effect: "NoSchedule"

  containers:
    - name: nginx
      image: nginx